# 业务规则/

定下来的业务逻辑。**业务代码变动前先看这里**，确认没破坏规则；
规则改动时同步更新这里。代码改动 + 文档改动**同一次提交**。

## 文件

- `工时计算.md` — calcNet / floorTo30Min / isEntryCounted / 补时长
- `推荐下班时间.md` — Dashboard 顶部推荐卡算法

后端存储与备份规则见 `../../backend/业务规则/数据存储与备份.md`。
