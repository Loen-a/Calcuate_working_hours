# 2026-07-12 本地后端与数据库

## 目标与架构

将原本依赖浏览器存储的单页应用改为本机单用户架构：FastAPI 同时提供构建后的单文件前端和 `/api/*`，SQLite `backend/data/workhours.db` 作为工时记录、主题与节假日缓存的唯一持久化来源。前端继续负责净工时、月度统计、走势图和推荐下班时间等派生计算。

Python 依赖使用项目内 Poetry 环境 `backend/.venv`，不修改全局 Python 环境。前端、后端与各自文档继续按 `frontend/`、`backend/`、`doc/frontend/`、`doc/backend/` 分离。

## 主要改动

- 后端增加数据库初始化、repository、service 和 FastAPI API 路由。
- 前端改用 API 读取和保存 entries、主题、节假日与备份，不再把 `localStorage` 当作正式数据源。
- `GET /` 提供 `frontend/dist/index.html`；构建产物不存在时返回带构建命令的 `503`。
- 启动命令固定为 `poetry -C backend run uvicorn backend.main:app --host 127.0.0.1 --port 8000`。

## 旧数据迁移与备份

- version 1 JSON 只替换 entries，保留当前主题和节假日缓存。
- version 2 JSON 备份和恢复 entries、主题、节假日缓存三类数据。
- version 2 在单个事务中恢复；完整校验或写入失败时全部回滚。
- 导出文件名精确到秒，格式为 `workhours-YYYY-MM-DD-HH-mm-ss.json`。

## 已执行验证

- `poetry -C backend run pytest tests/api/test_frontend.py -v`：2 passed；缺少路由时先观察到 2 个预期 `404` 失败，实现后通过。
- `poetry -C backend run pytest -v`：61 passed，1 个来自 FastAPI TestClient 的上游弃用警告。
- `npm --prefix frontend test`：3 个测试文件、10 个测试全部通过。
- `npm --prefix frontend run build`：`tsc --noEmit` 与 Vite 构建通过，生成单文件 `frontend/dist/index.html`。
- `poetry -C backend env info --path`：输出 `.venv`，对应项目内 `backend/.venv`。
- `rg -n "workhours_v1|workhours_theme_v1|holidaycn_v1_|localStorage" frontend/src`：无匹配，生产源码不存在旧持久化调用。
- 启动固定 Uvicorn 命令后的 HTTP 冒烟：`GET /api/health` 返回 `status=ok`；`GET /` 返回 `200 text/html`、590822 字节，且包含应用挂载节点。验证后已停止服务并确认 8000 端口释放。

本日志只记录已实际执行的自动化与 HTTP 级验证。当前环境没有专用 Browser 插件，因此未记录真实浏览器交互验收；创建/编辑后刷新、主题持久化、浏览器重启、导入导出交互、后端断开时弹框与 localStorage 检查仍需手验。
